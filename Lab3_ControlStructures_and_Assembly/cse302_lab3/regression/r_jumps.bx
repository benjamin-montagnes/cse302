// structured jumps (break, continue)

mx = 30;
x = 1;
while (true) {
  print(-x);
  y = 0;
  while (y < mx) {
    if (x == y) { break; }
    y = y + 1;
    if ((x + y) % 3 == 0) { continue; }
    print(y);
  }
  x = x + 1;
  if (x == mx) {
    break;
  }
}